{
  "name": "CV",
  "version": "1.0.0",
  "description": "Petro Kozhushko | CV",
  "author": "pkozhushko",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "axios": "^0.19.2",
    "nuxt": "^2.0.0"
  },
  "devDependencies": {
    "node-sass": "^4.14.1",
    "push-dir": "^0.4.1",
    "sass-loader": "^8.0.2",
    "vue-particles": "^1.0.9"
  }
}
